# Exercício 04

mg = float(input("Digite a massa em gramas: "))
temp_inicial = float(input("Digite a temperatura inicial: "))
temp_final = float(input("Digite a temperatura final: "))
temp = (temp_inicial - temp_final)
numc = 1 # cal/g
energia = (mg * numc * temp)

print("A quantidade de energia necessária para atingir a variação de temperatura desejada é de %s cal." %(energia))
